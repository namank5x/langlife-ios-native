import SwiftUI

enum AppColors {
    static let accent = Color(.label)
    static let onAccent = Color(.systemBackground)
    static let accentSubtle = Color(.tertiarySystemGroupedBackground)
    static let micPulse = Color(red: 1.0, green: 0.72, blue: 0.22)
    static let micPulseHalo = Color(.systemGray2)
}
